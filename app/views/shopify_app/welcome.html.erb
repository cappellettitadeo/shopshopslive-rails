<!DOCTYPE html>
<html lang="en">
<head>
  <!-- Bootstrap core CSS -->
  <link href="https://stackpath.bootstrapcdn.com/bootstrap/4.1.1/css/bootstrap.min.css" rel="stylesheet">
  <script src="https://cdn.shopify.com/s/assets/external/app.js"></script>
  <script type="text/javascript">
      ShopifyApp.init({
          apiKey: "<%= @api_key %>",
          shopOrigin: "https://<%= @shop %>"
      });
  </script>
  <script src="https://ajax.googleapis.com/ajax/libs/jquery/2.1.4/jquery.min.js"></script>
  <style>
    html,
    body, .product-not-listed {
      width: 100%;
      height: 100%;
    }
    .product-not-listed, .tips{
      display: flex;
      flex-direction: column;
    }
    .product-not-listed{
      align-items: center;
      justify-content: center;
      background-image: url(<%= asset_path "background_image.jpg" %>);
      background-repeat: no-repeat;
      background-size: cover;
    }
    .black-title{
      color: black;
    }
    h1 {
      text-align: center;
      margin: 50px;
    }
    h2{
      text-align: center;
    }
    p{
      text-align: left;
      font-weight: 500;
      font-size: 18px;
      line-height: 5px;
    }
    .tips{
      margin-top: 50px;
      width: fit-content;
    }
    h1, h2, p, .tips{
      color: white;
    }
  </style>
</head>
<body>
<% if @products.present? %>
  <h1 class="black-title">Congrats! Your products are available on Shopshops now!</h1>
  <div>
    <table class="table table-striped">
      <thead>
      <tr>
        <th scope="col">#</th>
        <th scope="col"></th>
        <th scope="col">Product</th>
        <th scope="col">Inventory</th>
        <th scope="col">Vendor</th>
      </tr>
      </thead>
      <tbody>
      <% @products.each_with_index do |product, index| %>
        <tr>
          <th class="align-middle" scope="row"><%= index + 1 %></th>
          <td >
            <% if product.photos.present? %>
              <%= image_tag(product.photos.first.thumb_url, size: "50x50") %>
            <%else%>
              <%= image_tag("img_placeholder.png", size: "50x50")%>
            <%end%>
          </td>
          <td class="align-middle">
            <%= product.name%>
          </td>
          <td class="align-middle"><%= product.product_variants.where(available: true).sum(&:inventory) %></td>
          <td class="align-middle"><%= product.vendor.name_en %></td>
        </tr>
      <% end %>
      </tbody>
    </table>
  </div>
<%else%>
  <% if @product_ids.present? %>
    <h1 class="black-title">We are examining your products, please return later.</h1>
  <%else%>
    <div class="product-not-listed">
      <h1>You've just connected with Shopshops.</h1>
      <h2>What's next?</h2>
      <div class="tips">
        <p> 1. Click on "Products" tab on the left side</p>
        <p> 2. Choose the products you want to list on Shopshops</p>
        <p> 3. Click on "Actions" -> "Make products available"</p>
        <p> 4. Check the box for "shopshops_sales"</p>
        <p> 5. Click on "Make products available"</p>
      </div>
    </div>
  <%end%>
<%end%>
</body>
</html>
